<Project>
    <!-- Global version info -->
    <PropertyGroup>
        <VersionPrefix>0.0.2</VersionPrefix>
        <VersionSuffix Condition=" '$(VersionSuffix)' == '' ">dev</VersionSuffix>
    </PropertyGroup>
</Project>